import type { SceneConfig } from "../systems/transitions/scene-manager"

export const sceneRegistry: Record<string, SceneConfig> = {
  // Architecture stub for future specific scenes
  // e.g. "hero-launch": { id: "hero-launch", environmentId: "launch-pad", ... }
}
